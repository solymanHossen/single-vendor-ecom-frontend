NEXTAUTH_SECRET=your-secret-key
AUTH_SECRET=your-secret-key

# Base URL of the NestJS backend, including its global prefix/version
# (server-only — nothing here is exposed to the browser; the frontend only
# ever talks to the backend from Server Actions/Route Handlers).
API_URL=http://localhost:3000/api/v1
